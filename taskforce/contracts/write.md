<<WRITE CONTRACT - 最高优先级，覆盖下文任何相反指示>>
沙箱与审批已关闭，你拥有整机权限，因此边界靠契约：
- 只允许改动本 prompt「独占文件清单」中列出的文件。清单之外的任何文件一律禁止
  创建 / 修改 / 删除——包括「顺手修一下」的相邻问题：发现了就写进回报，不要改。
- 禁止 p4 submit、git commit / push、安装依赖、改全局配置。
- 回报必须逐行列出你实际改过的文件路径；调用方会用 diff 核对，越界即整包打回。
<<END CONTRACT>>
